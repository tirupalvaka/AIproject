{
  "IsEncrypted": false,
  "Values": {
    "AzureWebJobsStorage": "UseDevelopmentStorage=true",
    "FUNCTIONS_WORKER_RUNTIME": "python",

    // Engine endpoint (NOT the ingest- host)
    "KUSTO_INGEST_URI": "https://aix-dts.eastus2.kusto.windows.net",

    // DB / table / mapping that exist in ADX
    "ADX_DATABASE": "aixdb",
    "ADX_TABLE": "aix_scores",
    "ADX_MAPPING": "aix_scores_json",

    // Optional: if your code also supports this name, keep both:
    "ADX_INGEST_URL": "https://aix-dts.eastus2.kusto.windows.net"
  }
}

